
import { CollectionConfig } from 'payload/types'

export const ApiDocs: CollectionConfig = {
  slug: 'api-docs',
  admin: {
    useAsTitle: 'title',
  },
  access: {
    read: () => true,
  },
  fields: [
    {
      name: 'title',
      type: 'text',
      required: true,
    },
    {
      name: 'content',
      type: 'richText',
      required: true,
    },
  ],
}
